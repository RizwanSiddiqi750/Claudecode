# Agency Agents

233 specialized Claude Code subagents installed from
[msitarzewski/agency-agents](https://github.com/msitarzewski/agency-agents)
(MIT licensed — see `LICENSE-agency-agents.txt`).

Generated via that repo's `scripts/install.sh --tool claude-code`. To update
after upstream changes, re-clone the source repo and re-run:

```bash
./scripts/install.sh --tool claude-code --path /path/to/this/repo/.claude/agents
```
